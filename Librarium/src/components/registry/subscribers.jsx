import React from 'react';
import View from '../shared/view';

export default function Subscribers() {
	return (
		<View title="Subscribers">
			<span>A table with one row per subscriber showing data for their latest subscription.</span>
		</View>
	);
}
